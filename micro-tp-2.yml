apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: nginx:1.28
    name: nginx
  - image: php:8.2-fpm
    name: php
  dnsPolicy: ClusterFirst
  restartPolicy: Always
